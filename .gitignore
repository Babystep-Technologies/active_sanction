/.bundle/
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
*.gem
/spec/reports/
/tmp/
/Gemfile.lock
/spec/examples.txt

# The documentation site's build output and its own bundle (#103). The site is
# published from a workflow rather than from a branch, so nothing it generates
# belongs in the tree.
/site/_site/
/site/.jekyll-cache/
/site/.jekyll-metadata
/site/Gemfile.lock
/site/vendor/
