---
title: Explanation
description: How matching works, and why the defaults are what they are.
sidebar:
  order: 1
  # The group heading in the sidebar is already this page's title, so listing
  # it underneath repeats the word and buys nothing. Hidden from the tree,
  # not from the site: the landing page's cards still link here, and so does
  # every cross-reference between quadrants.
  hidden: true
---

Why the library works the way it does. Discursive rather than instructive:
an explanation never tells you to do anything. Where you want to *act* on
what a page here argues, [Guides](/active_sanction/how-to/) has the steps.

- [How matching works](/active_sanction/explanation/how-matching-works/) —
  the pipeline from a raw query to a scored, explained result: normalizing a
  name, retrieving candidates, blending four algorithms and a phonetic pass,
  adjusting for identifiers, and why the default threshold is 75.
